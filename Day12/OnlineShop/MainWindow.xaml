<Window x:Class="OnlineShop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OnlineShop"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,20">
            <Button Content="Новый" Width="100" Height="35" Margin="0,0,10,0" Background="#4CAF50" Foreground="White" BorderThickness="0"/>
            <Button Content="Редактировать" Width="120" Height="35" Margin="0,0,10,0" Background="#2196F3" Foreground="White" BorderThickness="0"/>
            <Button Content="Удалить" Width="100" Height="35" Background="#F44336" Foreground="White" BorderThickness="0"/>
        </StackPanel>

        <!-- Фильтр -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Статус:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#555"/>
            <ComboBox x:Name="StatusFilter" Width="150" Height="28" SelectionChanged="StatusFilter_SelectionChanged">
                <ComboBoxItem Content="Все" IsSelected="True"/>
                <ComboBoxItem Content="Новый"/>
                <ComboBoxItem Content="Оплачен"/>
                <ComboBoxItem Content="Доставлен"/>
            </ComboBox>
        </StackPanel>

        <DataGrid x:Name="OrdersGrid" Grid.Row="2" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  Background="White" 
                  BorderThickness="1" 
                  BorderBrush="#DDD"
                  RowHeight="35"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#F0F0F0">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F8F9FA"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#DDD"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding Client}" Width="*"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="150"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Amount, StringFormat={}{0:C}}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
